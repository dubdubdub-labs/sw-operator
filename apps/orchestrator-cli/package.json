{
  "name": "@repo/orchestrator-cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "orchestrator": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -w -p tsconfig.build.json",
    "start": "bun src/cli.ts",
    "test": "echo 'no tests'",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "@repo/providers-codesandbox": "workspace:*",
    "@repo/credentials": "workspace:*",
    "@repo/orchestrator": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*"
  }
}
