"use client";

import { Grid } from "@/components/grid";

export default function Home() {
  return (
    <div className="bg-red-500">
      <Grid colDefs={[]} context={{}} rowData={[]} />
    </div>
  );
}
